# PRD — AI Daily

หนังสือพิมพ์ AI สำหรับคนทั่วไปและ AI Fullstack Developer

- เวอร์ชัน: 0.6 — ใช้ daisyUI เป็นพื้นฐาน UI
- วันที่: 20 กันยายน 2026
- ชื่อ AI Daily เป็นชื่อใช้งานชั่วคราว
- เอกสารนี้เป็นข้อกำหนดผลิตภัณฑ์ ขณะนี้เริ่มพัฒนา UI prototype ด้วยข้อมูลจำลอง ยังไม่เปิดระบบ production

## 1. เป้าหมาย

สร้างหนังสือพิมพ์ออนไลน์ที่ช่วยให้ผู้อ่านเข้าใจข่าว AI และตัดสินใจนำความรู้ไปใช้ได้ ผ่านสองมุมมองในเว็บเดียว ได้แก่ “ข่าว AI” สำหรับคนทั่วไป และ “สำหรับนักพัฒนา” สำหรับผู้ทำงานด้าน AI และ Fullstack

รวบรวมข่าวจากแหล่งต้นฉบับ ผู้เชี่ยวชาญ บุคคลในวงการ งานวิจัย และประกาศเทคโนโลยีใหม่ นำมาคัดเลือก รวมเรื่องซ้ำ และเรียบเรียงเป็นภาษาไทย โดยทุกข่าวมีที่มาและแยกข้อเท็จจริงออกจากความคิดเห็น

ระบบรันตามตารางทุก 12 ชั่วโมง ตั้งค่า provider, model และ API key ของ Agent ได้ และตรวจสอบการใช้ token กับค่าใช้จ่ายย้อนหลังได้

## 2. ข้อกำหนดที่ตกลงแล้วและข้อเสนอเริ่มต้น

### ข้อกำหนดจากผู้ใช้

- รูปแบบหนังสือพิมพ์ออนไลน์ด้าน AI
- รองรับทั้งผู้อ่านทั่วไปและ AI Fullstack Developer
- รวมข่าว เทคโนโลยีใหม่ และมุมมองจากผู้เชี่ยวชาญหรือบุคคลในวงการ
- มีข่าวเครื่องมือ framework และวิธีทำงานที่ช่วยลดเวลา ต้นทุน และข้อผิดพลาด
- ส่งเสริมหลักการพัฒนาซอฟต์แวร์ที่อ่านง่าย ดูแลง่าย และไม่สร้างโค้ดเกินจำเป็น
- มี cron ทุก 12 ชั่วโมง และการตั้งค่า API key ของ Agent
- มีระบบคำนวณ token และค่าใช้จ่าย
- Admin ตั้งกฎกรองข่าวและเลือกตรวจเอง เผยแพร่อัตโนมัติ หรือแบบผสมได้
- ไม่เพิ่มค่าใช้จ่าย: ใช้เฉพาะบริการ/โมเดลที่อยู่ในโควตาฟรี ไม่เปิดแพ็กเกจเสียเงินหรือ fallback ที่คิดเงินอัตโนมัติ
- ยืนยันใช้ Cloudflare สำหรับหน้าเว็บและโดเมน ร่วมกับ Supabase Free สำหรับฐานข้อมูล Auth และงาน cron/ประมวลผล
- ยืนยันใช้ daisyUI ร่วมกับ Tailwind CSS เป็นพื้นฐาน UI ทั้งหน้าอ่านข่าวและหลังบ้าน โดยใช้เฉพาะส่วนที่ไม่เพิ่มค่าใช้จ่าย

### ค่าเริ่มต้นที่เสนอในร่างนี้

| เรื่อง | ค่าเริ่มต้น |
|---|---|
| เวลา cron | 07:00 และ 19:00 Asia/Bangkok |
| ความถี่ | ทุก 12 ชั่วโมงตามคำขอเดิม; การตรวจข่าวทุกชั่วโมงเป็นทางเลือกภายหลัง |
| การอ่าน | อ่านสาธารณะโดยไม่ต้องสมัครสมาชิกทั้งสองมุมมอง |
| การเผยแพร่ | เริ่มจาก Admin ตรวจอนุมัติ; เปิดอัตโนมัติได้สำหรับรายการที่ผ่านเกณฑ์ |
| ภาษา | เนื้อหาหลักภาษาไทย เก็บศัพท์เทคนิคและชื่อผลิตภัณฑ์ตามต้นฉบับ |
| การออกแบบ | หนังสือพิมพ์สมัยใหม่ พื้นขาวนวล ตัวอักษรเข้ม สีเน้นน้ำเงิน |
| UI framework | daisyUI + Tailwind CSS โดยปรับ theme ให้เข้ากับหนังสือพิมพ์และใช้ร่วมกันทั้งสองมุมมองกับหลังบ้าน |
| ระบบ | Next.js + TypeScript สำหรับ UI, static frontend บน Cloudflare และ Supabase Free สำหรับ PostgreSQL/Auth/Vault/Cron/Edge Functions |

ผู้ใช้ยืนยันแนวทาง Cloudflare + Supabase Free แล้ว ส่วนค่าเริ่มต้นอื่นเป็นข้อเสนอสำหรับพัฒนา การยืนยันนี้ไม่ได้เปิดใช้บริการ deploy เปลี่ยน DNS หรืออนุมัติค่าใช้จ่ายเพิ่มเติม

ผู้ใช้มีบัญชี Cloudflare และโดเมนอยู่แล้ว แต่ยังไม่ระบุ hostname, สถานะ DNS zone หรือแพ็กเกจ Workers; ให้ตรวจข้อมูลเหล่านี้ก่อนเตรียม deployment โดยไม่เปลี่ยน DNS ในขั้น prototype

## 3. กลุ่มผู้ใช้และงานที่ต้องทำให้สำเร็จ

| กลุ่ม | สิ่งที่ต้องการ | ผลลัพธ์ที่ผลิตภัณฑ์ควรช่วยให้เกิด |
|---|---|---|
| คนทั่วไป | เข้าใจว่าเกิดอะไรขึ้นและเกี่ยวกับชีวิตหรืองานอย่างไร | อ่านสรุปประจำรอบได้ภายในเป้าหมายประมาณ 5 นาที |
| AI Fullstack Developer | ตามเครื่องมือ framework release และ workflow ที่เกี่ยวข้อง | ตัดสินใจได้ว่าควรลอง ใช้ต่อ หรือเพียงติดตาม พร้อมเหตุผลและหลักฐาน |
| Admin/บรรณาธิการ | ควบคุมแหล่งข่าว คุณภาพ การเผยแพร่ และต้นทุน | ตรวจข่าว รันงาน ตรวจความผิดพลาด และแก้การตั้งค่าได้จากหลังบ้าน |

มุมมองนักพัฒนาเป็นการเลือกเนื้อหาและระดับรายละเอียด ไม่ใช่สิทธิ์เข้าถึงข้อมูลลับของระบบ

## 4. ประสบการณ์สองมุมมอง

### 4.1 ข่าว AI สำหรับคนทั่วไป

- ข่าวพาดหัวและข่าวสำคัญประจำรอบ
- ข่าวล่าสุด พร้อมเวลาเผยแพร่ต้นฉบับและเวลาที่ระบบอัปเดต
- สรุป “เกิดอะไรขึ้น / ทำไมสำคัญ / ใครได้รับผลกระทบ”
- เทคโนโลยีและเครื่องมือที่เกี่ยวข้องกับการใช้งานทั่วไป
- มุมมองคนในวงการ พร้อมป้ายความคิดเห็นหรือบทสัมภาษณ์
- คำอธิบายศัพท์ AI แบบสั้นเมื่อจำเป็น
- ฉบับสรุปเช้าและเย็น พร้อมลิงก์ไปยังข่าวที่เกี่ยวข้อง

### 4.2 สำหรับนักพัฒนา

- ข่าวที่เกี่ยวข้องกับ AI engineering, frontend, backend, database, deployment และ developer tooling
- Tools & Framework Radar: งานที่เครื่องมือช่วยแก้ ข้อจำกัด วิธีเริ่มทดลอง และแหล่งข้อมูลทางการ
- Release & Breaking Changes: เวอร์ชันที่เกี่ยวข้อง ผลกระทบ และแนวทางย้ายที่มีเอกสารรองรับ
- Workflow Playbook: ขั้นตอนทำงาน prerequisites ตัวอย่าง และวิธีตรวจว่าทำสำเร็จ
- Engineering Practices: การออกแบบโค้ด การทดสอบ การจัดการ context และการลดงานแก้ซ้ำ
- ตัวกรองตามภาษา framework และหัวข้อที่สนใจ
- บันทึกรายการอ่านภายหลังและความสนใจไว้ใน browser สำหรับ MVP; ยังไม่ sync ข้ามอุปกรณ์

### 4.3 ใช้ข่าวและข้อเท็จจริงร่วมกัน

ข่าวหนึ่งเรื่องมีข้อมูลต้นฉบับและข้อเท็จจริงชุดเดียว พร้อมบทสรุปทั่วไป และส่วนผลกระทบต่อนักพัฒนาเฉพาะเมื่อเกี่ยวข้อง

ไม่สร้างบทความซ้ำสองชิ้นเพียงเพราะมีสองมุมมอง ผู้อ่านสลับมุมมองในหน้าข่าวเดิมได้ ข่าวทั่วไปที่ไม่มีสาระทางเทคนิคไม่ต้องสร้างเนื้อหานักพัฒนาเพิ่มเติม

หน้าแรกแต่ละมุมมองจัดลำดับข่าวต่างกันได้ แต่ต้องไม่เปลี่ยนข้อเท็จจริงตามกลุ่มผู้อ่าน

## 5. โครงสร้างหน้าและแนวทางดีไซน์

| หน้า | องค์ประกอบหลัก |
|---|---|
| หน้าแรก / ข่าว AI | ชื่อหนังสือพิมพ์ วันที่ ฉบับล่าสุด ข่าวพาดหัว ข่าวรอง ข่าวล่าสุด และหมวดหมู่ |
| สำหรับนักพัฒนา | ข่าวเด่นตาม stack, Tools Radar, releases และ playbooks |
| รายละเอียดข่าว | พาดหัว สรุป เนื้อหา ประเภทข่าว แหล่งอ้างอิง เวลา ส่วนเทคนิคเมื่อเกี่ยวข้อง และประวัติแก้ไข |
| เครื่องมือและ framework | ค้นหาและกรองตามงาน ภาษา สถานะการประเมิน และรูปแบบค่าใช้จ่าย |
| Workflow Playbook | ปัญหา ขั้นตอน ตัวอย่าง วิธีตรวจผล ข้อจำกัด และเวอร์ชันที่ตรวจสอบ |
| ค้นหา | ค้นหาหัวข้อ สรุป ชื่อเครื่องมือ บุคคล และหมวดหมู่ |
| หลังบ้าน | ข่าว แหล่งข้อมูล Agents งาน cron Usage & Budget และ audit log |

แนวทางภาพรวม:

- ใช้ daisyUI เป็นพื้นฐานสำหรับองค์ประกอบมาตรฐาน เช่น button, input, select, tabs, badge, table, alert และ modal โดยตรวจ API/วิธีติดตั้งตามเวอร์ชันจริงก่อนใช้
- ใช้ theme และ design tokens ชุดเดียวสำหรับสี ตัวอักษร ขอบ และสถานะ ลด CSS ที่ซ้ำกับ framework; ใช้ Tailwind/CSS เพิ่มเฉพาะ layout หนังสือพิมพ์และรายละเอียดที่จำเป็น
- รักษาลำดับพาดหัว คอลัมน์ข่าว พื้นขาวนวล และความอ่านง่าย แทนการเปลี่ยนหน้าอ่านข่าวทั้งหมดเป็นชุดการ์ดตามค่าเริ่มต้น
- ใช้ semantic HTML และดูแล keyboard/focus/accessible labels ของ interaction จริง; การใช้ component styles ไม่ทดแทนการตรวจ accessibility
- ใช้เฉพาะ core/components/themes ที่ไม่เพิ่มค่าใช้จ่าย ไม่ซื้อ template หรือชุด UI เสริม
- ใช้รูปแบบหนังสือพิมพ์ร่วมกันทั้งสองมุมมอง พร้อมแถบสลับ “ข่าว AI / สำหรับนักพัฒนา”
- Desktop มีข่าวพาดหัวเด่น ข่าวรอง และคอลัมน์ข่าวล่าสุด; mobile เรียงเป็นคอลัมน์เดียว
- ใช้ Sarabun เป็นฟอนต์เนื้อหา และ monospace สำหรับตัวอย่างโค้ด
- ส่วนเทคนิคอยู่ในหัวข้อแยกที่เปิดอ่านได้ ไม่แทรกศัพท์หนักในบทสรุปทั่วไป
- แสดงสถานะ “อัปเดตสำเร็จล่าสุด” และ “รอบถัดไป” จากข้อมูลระบบจริง
- ออกแบบให้ใช้ keyboard ได้ มี focus ที่มองเห็น และใช้ข้อความประกอบสถานะสี
- รองรับหน้าไม่มีข่าว กำลังโหลด ค้นหาไม่พบ และระบบอัปเดตล้มเหลว

## 6. แหล่งข้อมูลและนโยบายบรรณาธิการ

Admin เพิ่มแหล่งข้อมูลแบบ RSS, API หรือช่องทางที่เจ้าของแหล่งข้อมูลอนุญาตได้ ระบุประเภท ภาษา URL วิธีเข้าถึง และบุคคลหรือองค์กรที่เกี่ยวข้อง

เริ่มต้นด้วยประมาณ 10–20 แหล่งที่คัดเลือก เน้นเอกสารทางการ engineering blog, release notes, repository releases, งานวิจัย และบทสัมภาษณ์ต้นฉบับ รายชื่อจริงต้องตรวจความพร้อมเข้าถึงก่อนเปิดใช้

- ข้อมูลจากบุคคลในวงการต้องเชื่อมกับต้นฉบับที่ระบุตัวตนได้
- แยกป้าย ข่าว / ความคิดเห็น / งานวิจัย / ประกาศบริษัท / แนวปฏิบัติ
- ไม่ใช้ยอดผู้ติดตามหรือ GitHub stars เป็นหลักฐานยืนยันคุณภาพ
- คำกล่าวอ้างด้านประสิทธิภาพต้องระบุว่าใครทดสอบ ภายใต้เงื่อนไขใด และมีข้อจำกัดอะไร
- สถานะหลักฐานของเครื่องมือ: “ข้อมูลผู้พัฒนา”, “รีวิวภายนอก”, “ทดลองภายใน”
- แสดง “ทดลองภายใน” ได้เมื่อมีบันทึกการทดลองจริงเท่านั้น
- ข่าวจากหลายแหล่งเกี่ยวกับเหตุการณ์เดียวกันรวมเป็นเรื่องเดียวได้ โดยรักษาลิงก์อ้างอิงที่เกี่ยวข้อง
- ความขัดแย้งของแหล่งข้อมูลต้องแสดงหรือส่งเข้าคิวตรวจ ไม่ให้ AI เลือกข้อสรุปโดยไม่มีหลักฐาน
- เรียบเรียงสรุปและลิงก์กลับต้นฉบับ ไม่คัดลอกบทความเต็มหรือข้าม paywall
- ข้อความจากข่าวหรือโพสต์ถือเป็นข้อมูล ไม่ใช่คำสั่งเปลี่ยนการทำงานหรือเข้าถึง secrets ของ Agent
- บันทึกการแก้ไขข่าวที่กระทบข้อเท็จจริง และมีสถานะถอนการเผยแพร่
- แหล่งที่ดึงจริงมีวิธีดึงหนึ่งแบบจากสามแบบ: `feed_only` (feed มีเนื้อพอสรุป), `feed_then_page` (feed เป็น teaser หรือว่าง ต้องตามไปดึงหน้าเต็ม), `json_api` (GitHub Releases อ่านผ่าน REST API เพราะ robots.txt ห้าม `.atom` ทุกเส้นทาง) · ค่าที่สี่ `manual` ไม่ใช่วิธีดึงแต่เป็นโต๊ะบรรณาธิการ ไม่มี feed และเปิดใช้ไม่ได้
- การดึงจากโดเมนหนึ่ง ไม่ว่าจะเป็น feed ของแหล่งเองหรือการตามลิงก์ไปหน้าต้นฉบับ ทำได้เฉพาะโดเมนใน allowlist ซึ่งเป็นรายชื่อแยกจากรายชื่อ feed · จับคู่ที่ขอบ label คือ host ตรงตัวหรือ subdomain ของโดเมนที่อนุญาต (`www.openai.com` ผ่าน, `evil-openai.com` ไม่ผ่าน)
- Admin เพิ่มแหล่งใหม่ แก้รายละเอียด และ**หยุดดึงรายแหล่ง**ได้จากหลังบ้าน (ลบแหล่งทิ้งไม่ได้ เพราะข่าวที่เผยแพร่แล้วอ้างถึงแหล่งนั้นอยู่) · เพิ่มและถอนโดเมนใน allowlist ได้ แต่ถอนไม่ได้ถ้ายังมีแหล่งที่เปิดใช้ดึงจากโดเมนนั้น · ทุกการเปลี่ยนผ่าน RPC ที่จำกัดสิทธิ์ผู้ดูแลและลง audit log พร้อมระบุว่าแตะแหล่งหรือโดเมนใด

แนวทางภาษา: ใช้ภาษาไทยชัดเจน ตรงประเด็น ลดคำอวยและประโยคสำเร็จรูป อธิบายด้วยตัวอย่างที่มีที่มา ไม่แต่งประสบการณ์ใช้เครื่องมือหรือผลทดสอบ และระบุว่าเป็นเนื้อหาที่ใช้ AI ช่วยเรียบเรียง

### 6.1 การกรองข่าวโดย Admin และระบบอัตโนมัติ

แยก “กฎคัดเลือกข่าว” ออกจาก “โหมดเผยแพร่” เพื่อให้ระบบดึงและคัดข่าวอัตโนมัติได้แม้ Admin ต้องตรวจบทความก่อนเผยแพร่

| โหมด | การทำงาน |
|---|---|
| ตรวจเอง (Manual) | ระบบดึง กรอง และเตรียมร่าง; Admin อ่านต้นฉบับ แก้ไข และอนุมัติก่อนขึ้นเว็บ |
| อัตโนมัติ (Auto) | ระบบเผยแพร่เฉพาะข่าวที่ผ่านกฎและเกณฑ์คุณภาพ; รายการที่ไม่แน่ชัดส่งเข้าคิวตรวจ |
| แบบผสม (Hybrid) | ตั้ง Manual/Auto แยกตามแหล่งข่าวหรือหมวด เช่น release notes ทางการเป็น Auto แต่ความคิดเห็นและคำกล่าวอ้างที่ยังไม่มีหลักฐานเป็น Manual |

ค่าเริ่มต้นยังเป็น Manual ตามตารางข้อเสนอ เมื่อทดสอบคุณภาพแล้วแนะนำ Hybrid โดย Admin เลือกเปิด Auto เฉพาะกลุ่มที่เหมาะสม

กฎคัดเลือกที่ตั้งค่าได้:

- แหล่งข่าวหรือบุคคลที่เปิดใช้ และรายการที่ต้องการตัดออก
- คำสำคัญที่ต้องมี/ห้ามมี พร้อมระบุว่าจะค้นในหัวข้อหรือเนื้อหา
- หมวดหมู่ ภาษา อายุข่าวสูงสุด และความเกี่ยวข้องกับ AI
- กลุ่มผู้อ่านทั่วไป/นักพัฒนา รวมถึงภาษาโปรแกรมและ framework ที่เกี่ยวข้อง
- จำนวนข่าวสูงสุดต่อแหล่งและต่อรอบ เพื่อควบคุมความซ้ำและต้นทุน
- กฎตรวจข่าวซ้ำ ซึ่งแยก URL ซ้ำออกจากข่าวคนละ URL ที่เล่าเหตุการณ์เดียวกัน

ใช้กฎที่ไม่ต้องพึ่งโมเดลก่อนเรียก AI ส่วนการจัดหมวดและประเมินความเกี่ยวข้องด้วย AI ต้องบันทึกเหตุผล ไม่ใช้คะแนนความมั่นใจที่โมเดลให้ตัวเองเป็นเกณฑ์เผยแพร่เพียงอย่างเดียว

ลำดับการตัดสิน: ปิดการเผยแพร่อัตโนมัติทั้งระบบหรือหยุดบทความโดย Admin มีผลก่อนเสมอ จากนั้นตรวจเงื่อนไขห้ามเผยแพร่และกฎตัดออก แล้วใช้โหมดที่กำหนดกับแหล่งข่าว/หมวด หากกฎขัดกันให้ Manual มีสิทธิ์เหนือ Auto และรายการไม่มีโหมดเฉพาะใช้ค่าเริ่มต้นของระบบ

ไม่เผยแพร่อัตโนมัติเมื่อขาดแหล่งอ้างอิงที่ตรวจสอบได้ แหล่งข้อมูลขัดแย้ง ข้อกล่าวอ้างสำคัญไม่มีหลักฐาน หรือการตรวจที่จำเป็นทำงานล้มเหลว; ส่งเข้าคิวตรวจพร้อมเหตุผล รายการนอกหัวข้อหรือซ้ำชัดเจนเก็บเป็น excluded พร้อมเหตุผลแทนการสร้างร่าง

### 6.2 หน้าคัดข่าวและการควบคุม

- แยกสถานะ: รอตรวจ / เผยแพร่แล้ว / ตัดออก / ปฏิเสธ / ถอนเผยแพร่ และกรองตามแหล่ง หมวด กลุ่มผู้อ่าน วันที่ และเหตุผลได้
- แสดงร่างข้างต้นฉบับและแหล่งอ้างอิง พร้อมกฎและเวอร์ชันนโยบายที่ทำให้รายการเข้าคิว
- Admin แก้เนื้อหา/หมวด/กลุ่มผู้อ่าน อนุมัติ ปฏิเสธ หรือถอนเผยแพร่ได้ โดยการปฏิเสธและถอนมีเหตุผลบันทึกไว้
- บทความที่ปฏิเสธหรือถอนจะไม่กลับมาเผยแพร่อัตโนมัติจากการรันซ้ำ; Admin ต้องเปิดตรวจใหม่อย่างชัดเจน
- มีปุ่มหยุด Auto ทั้งระบบทันที โดยยังดึงและเตรียมร่างได้ งานที่กำลังรันต้องตรวจสวิตช์และนโยบายล่าสุดอีกครั้งก่อนเผยแพร่
- มีการทดลองกฎกับข้อมูลที่ดึงมาแล้ว แสดงผลว่าจะผ่าน ตัดออก หรือรอตรวจก่อนบันทึก โดยไม่เผยแพร่จริง; หากทดลองต้องเรียก AI เพิ่มให้แสดงประมาณการและบันทึก usage
- การเปลี่ยนโหมดหรือกฎไม่มีผลเผยแพร่ร่างเก่าย้อนหลังทันที ต้องสั่งประเมินรายการค้างใหม่และผ่านเกณฑ์ก่อน
- ข่าวที่เผยแพร่แล้วจะไม่เปลี่ยนเนื้อหาจากการดึงซ้ำโดยเงียบ ๆ; การแก้ข้อเท็จจริงสำคัญสร้าง revision รอตรวจ
- การเปลี่ยนกฎ การตัดสินข่าว และสวิตช์ Auto มี audit log ระบุผู้กระทำและเวลา

## 7. Tools Radar และ Workflow Playbook

### การ์ดเครื่องมือ

ข้อมูลขั้นต่ำ: ชื่อ งานที่ช่วยแก้ ภาษา/stack ที่เกี่ยวข้อง เวอร์ชันหรือวันที่ตรวจสอบ ลิงก์เอกสาร วิธีทดลอง ข้อจำกัด รูปแบบค่าใช้จ่าย และประเภทหลักฐาน

ข้อเสนอ “ควรลอง / ติดตาม / ยังไม่เหมาะกับกรณีนี้” ต้องมีเหตุผลที่อ้างอิงความต้องการและหลักฐาน ห้ามให้คะแนนความน่าใช้ที่ดูแม่นยำโดยไม่มีเกณฑ์รองรับ

### Playbook

ข้อมูลขั้นต่ำ: สถานการณ์ที่เหมาะ สิ่งที่ต้องเตรียม ขั้นตอน ผลลัพธ์ที่คาดหวัง วิธีตรวจผล ข้อผิดพลาดที่พบบ่อย และแหล่งอ้างอิง

ตัวอย่างหัวข้อเริ่มต้น:

1. เพิ่ม feature โดยกำหนด acceptance criteria และอ่านรูปแบบโค้ดเดิมก่อนแก้
2. แก้บั๊กด้วยการทำซ้ำอาการ ตรวจสาเหตุ และเพิ่ม regression test เมื่อเหมาะสม
3. ลด context โดยเลือกไฟล์และข้อมูลที่จำเป็นโดยรักษาข้อกำหนดสำคัญ
4. เปรียบเทียบ prompt/model ด้วยชุดตัวอย่างเดียวกันและเกณฑ์คุณภาพเดียวกัน
5. ตรวจเส้นทางสำคัญตั้งแต่หน้าเว็บถึง API และฐานข้อมูล

หลักคุณภาพที่ใช้คัดเลือกคำแนะนำ: KISS, YAGNI, การใช้ของเดิมซ้ำเมื่อเหมาะสม, ขอบเขตการเปลี่ยนแปลงชัดเจน, ไม่ซ่อน error, ทดสอบพฤติกรรมสำคัญ และอธิบายเหตุผลของการออกแบบ ไม่ใช้คำว่า “เหมือนคนเขียน” เป็นคะแนนแทนคุณภาพที่ตรวจได้

## 8. Pipeline และตารางการทำงาน

ลำดับหลัก:

1. สร้าง Run และตรวจสิทธิ์เริ่มงานกับงบประมาณ
2. ดึงข้อมูลใหม่จากแหล่งที่เปิดใช้ โดยเก็บ checkpoint แยกแต่ละแหล่ง
3. ทำ URL ให้เป็นรูปแบบมาตรฐาน ตรวจซ้ำและ content hash
4. กรองด้วยกฎทั่วไปก่อนใช้ AI เช่น ช่วงเวลาและรายการที่เคยประมวลผล
5. ให้ AI คัดความเกี่ยวข้องและจัดกลุ่มเรื่องเมื่อจำเป็น
6. สร้างสรุปทั่วไปและส่วนสำหรับนักพัฒนาเฉพาะข่าวที่เกี่ยวข้อง
7. ตรวจ schema แหล่งอ้างอิง วันที่ ตัวเลข และเงื่อนไขคุณภาพ
8. ส่งเข้าคิวตรวจหรือเผยแพร่ตามนโยบาย
9. สร้างฉบับสรุปจากข่าวที่เผยแพร่แล้ว พร้อมบันทึก usage และสถานะรอบ

MVP ใช้ cron `0 0,12 * * *` ใน UTC ตรงกับ 07:00 และ 19:00 เวลาไทย เป็นเวลาเริ่มประมวลผล ไม่ใช่เวลารับประกันว่าข่าวทั้งหมดจะเผยแพร่แล้ว

หน้าเว็บอ่านได้ตลอดเวลา แต่ความสดของข้อมูลขึ้นกับรอบดึงข่าว ระบบไม่อ้างว่าเป็นข่าวแบบ real-time

ทางเลือกภายหลัง: ตรวจข้อมูลใหม่ทุกชั่วโมง และคงฉบับสรุปใหญ่ทุก 12 ชั่วโมง โดยประมวลผลเฉพาะข้อมูลที่เปลี่ยนและต้องประเมินต้นทุนก่อนเปิดใช้

### ความทนทาน

- ใช้ run key ของรอบและฐานข้อมูลป้องกันการเริ่มงานซ้ำ พร้อม idempotency ระดับบทความและขั้นตอน
- ใช้ lock/lease ที่กู้คืนได้เมื่อ worker หยุดผิดปกติ และมี checkpoint สำหรับทำต่อ
- UI “Run now” ต้องไม่เริ่มรอบซ้อน หากมีงานอยู่ให้แสดงสถานะนั้น
- cron และ Run now ต้องใช้กลไกตรวจงานซ้ำและ lease ชุดเดียวกันฝั่ง backend ไม่อาศัย scheduler เป็นตัวรับประกันว่าไม่มีงานซ้อน
- retry เฉพาะความผิดพลาดชั่วคราว สูงสุดเริ่มต้น 3 attempts ต่อขั้นตอน พร้อม backoff
- authentication ผิดหรือ API key หมดสิทธิ์ให้หยุดการใช้ credential นั้นและแจ้ง Admin
- แต่ละแหล่งข้อมูลล้มเหลวได้โดยไม่ล้มทั้งรอบ สถานะรอบรองรับ success, partial และ failed
- กู้คืนข้อมูลจาก checkpoint โดยใช้ช่วงเวลาซ้อนเล็กน้อยและตรวจซ้ำ เพื่อไม่ข้ามข่าวช่วงที่ระบบล่ม
- รอบใหม่ล้มเหลวต้องคงข่าวเดิมและเวลาอัปเดตสำเร็จเดิม

## 9. การตั้งค่า Agent และ API key

หน้าที่เริ่มต้นของ Agent คือคัดข่าว เรียบเรียง และตรวจคุณภาพ แต่ไม่บังคับว่าทุกข่าวต้องเรียกโมเดลแยกครบสามครั้ง การรวมบางขั้นตอนทำได้เมื่อผลประเมินผ่านเกณฑ์

Admin ตั้งค่าได้: ชื่อ Agent, หน้าที่, provider, model, credential reference, prompt version, output token limit, timeout, retry limit และสถานะเปิดใช้

- credential หนึ่งรายการใช้ร่วมหลาย Agent ได้ และ Agent เลือกคนละ credential ได้
- เพิ่ม เปลี่ยน ลบ และทดสอบการเชื่อมต่อได้; การทดสอบที่เรียกโมเดลต้องบันทึก usage ด้วย
- หลังบันทึกแสดงชื่ออ้างอิงและค่าปิดบัง ไม่ส่ง key เต็มกลับ browser
- เก็บ secret เข้ารหัส; สิทธิ์ถอดรหัสจำกัดเฉพาะ backend ที่จำเป็น
- ไม่บันทึก key ใน logs, traces, analytics หรือข้อความ error
- ระบุเมื่อ credential ที่ต้องการลบยังถูก Agent ใช้อยู่ และให้เปลี่ยน reference หรือปิด Agent ก่อน
- บันทึกผู้เปลี่ยนการตั้งค่าและเวลา โดยไม่เก็บ key ใน audit log
- แต่ละรอบใช้ snapshot ของการตั้งค่า เพื่ออธิบายผลลัพธ์ย้อนหลังได้

## 10. Token, ค่าใช้จ่าย และงบประมาณ

### ขอบเขต

รายงานนับการเรียก API ที่ระบบนี้ดำเนินการ ไม่สามารถมองเห็นการใช้ token ใน IDE หรือบัญชีบริการอื่นของผู้ใช้โดยอัตโนมัติ การเชื่อม telemetry จากเครื่องมือภายนอกเป็นงานภายหลัง

### ข้อมูลต่อการเรียก

เก็บ invocation ID, provider request ID เมื่อมี, run, article, Agent, model, credential reference แบบไม่ใช่ secret, prompt version, attempt, เวลา, latency, สถานะ, usage และ pricing snapshot

แยก input, output, cache read/write และ reasoning ตามข้อมูลที่ provider ส่งกลับ โดยมี mapping ป้องกันการนับซ้ำ เพราะบาง provider รวม cached input หรือ reasoning ไว้ในยอดประเภทหลักอยู่แล้ว

ระดับความเชื่อมั่นของ usage: provider-reported / estimated / unavailable หากไม่มีข้อมูลห้ามแสดงว่าใช้ศูนย์ token

สูตรแนวคิด:

`ค่าใช้จ่าย = ผลรวม (token แต่ละประเภทที่ไม่ซ้ำกัน ÷ 1,000,000 × ราคาประเภทนั้นต่อหนึ่งล้าน token)`

การคิดเงินจริงต้องรองรับหน่วยและเงื่อนไขของ provider รวมถึง tier หรือค่าเรียกเครื่องมือหากมี ค่าโฮสต์และค่าบริการภายนอกแสดงแยกจากค่า token

### หน้ารายงาน

- ต่อข่าว ต่อ Agent ต่อ model ต่อ credential reference ต่อรอบ และรายวัน/เดือน
- ประมาณการก่อนรันเทียบกับ usage หลังรัน
- ต้นทุนเฉลี่ยต่อข่าวที่เผยแพร่ และต้นทุนงานที่ไม่ผ่าน/ต้องแก้ซ้ำ
- จำนวนข่าวทั่วไปและข่าวที่มีส่วนเทคนิค พร้อมวิธีจัดสรรต้นทุนร่วมอย่างชัดเจน
- ยอดรวมต้องไม่นับการเรียกเดียวซ้ำเมื่อข่าวปรากฏทั้งสองมุมมอง
- เก็บราคาที่ใช้คำนวณ ณ เวลานั้น การอัปเดตราคาต้องไม่แก้ยอดประวัติเดิมโดยเงียบ ๆ
- ใช้ USD เป็นหน่วยต้นทาง; หากแสดง THB ให้แสดงอัตราและเวลาที่ใช้อ้างอิง
- ยอดต้นทุนเป็นค่าคำนวณเพื่อควบคุมการใช้งาน บิล provider เป็นแหล่งยืนยันยอดชำระจริง

### การควบคุมงบ

- ตั้งเพดานจำนวนข่าว token output ต่อการเรียก งบต่อรอบ และงบต่อเดือน
- แจ้งเตือนในหลังบ้านเมื่อถึง 80% ของงบเป็นค่าเริ่มต้น
- ตรวจและสำรองงบแบบ atomic ก่อนส่งแต่ละ request รวม retry และงานทดสอบการเชื่อมต่อ
- กระทบยอดเงินสำรองเมื่อได้รับ usage; หากผลลัพธ์ไม่แน่นอนให้แสดง pending/unknown ไม่คืนงบโดยสมมติว่าไม่มีค่าใช้จ่าย
- เมื่อถึงเพดานให้หยุดเริ่มงานใหม่ งานที่ส่งไปแล้วอาจยังมีค่าใช้จ่าย
- ไม่สลับไปใช้โมเดลแพงขึ้นโดยไม่มีนโยบาย fallback และงบรองรับ
- reset รอบเดือนตาม Asia/Bangkok และระบุ timezone ในรายงาน

## 11. System spec ที่เลือก

| ส่วน | ข้อเสนอและเหตุผล |
|---|---|
| Web และ backend | Next.js + TypeScript สำหรับ UI หน้าอ่านและหลังบ้าน; งานที่ต้องใช้ server อยู่ใน Supabase Edge Functions/Database Functions |
| UI framework | daisyUI + Tailwind CSS; ตรวจความเข้ากันได้กับ Next.js และเวอร์ชัน dependencies ก่อนติดตั้ง |
| Web hosting | static frontend บน Cloudflare โดยตรวจ Pages/Workers Static Assets กับ build ที่ใช้; การเพิ่ม SSR ต้องพิสูจน์ว่าอยู่ใน Free limits ก่อน |
| ฐานข้อมูลและ Admin login | Supabase Free: PostgreSQL + Auth พร้อม RLS และสิทธิ์ Admin |
| Secrets | Supabase Vault สำหรับ API key ที่ตั้งค่าผ่านเว็บ; infrastructure secrets อยู่ใน secret settings ของ runtime ที่ใช้ |
| งานตามรอบ | Supabase Cron เรียก Edge Functions ตามรอบทุก 12 ชั่วโมง; แบ่ง batch และบันทึก checkpoint ให้ทำต่อได้ใน Free limits |
| AI integration | AI SDK สำหรับ provider/model registry โดยตรวจ compatibility ของ provider ที่เลือก |
| Validation | Zod ตรวจข้อมูลจากภายนอกและ structured output |
| Prompt evaluation | Promptfoo ใช้ชุดตัวอย่างและเกณฑ์ประเมินก่อนเปลี่ยน prompt/model |
| การตรวจเว็บ | Playwright สำหรับเส้นทางใช้งานสำคัญ |
| Usage ledger | เก็บในฐานข้อมูลแอปเป็นแหล่งหลักสำหรับการควบคุมงบ |
| Observability | structured logs ใน MVP; เพิ่ม Langfuse เมื่อจำเป็นต้องวิเคราะห์ traces โดยปิดบังข้อมูลอ่อนไหว |

เริ่มด้วย pipeline ที่กำหนดขั้นตอนชัดเจน ไม่ต้องมี vector database, queue cluster หรือ multi-agent orchestration จนกว่าปริมาณงานหรือกรณีใช้งานพิสูจน์ว่าจำเป็น

ขั้น prototype คง Next.js และข้อมูลจำลอง ส่วน production ต้องตรวจ static export, URL ของข่าวใหม่ และความสดของข้อมูลให้ตรงกับการอัปเดตทุก 12 ชั่วโมง โดยไม่สมมติว่า server features ของ Next.js จะใช้ได้บน static hosting ให้ทดสอบ Supabase Edge Functions บน runtime จริง รวม dependencies, authentication, CPU และระยะเวลาทำงาน; หากรอบประมวลผลเกินขีดจำกัดให้แบ่งงานที่ resume ได้

### 11.1 ข้อตกลง Cloudflare + Supabase Free

ผู้ใช้ยืนยันแนวทางนี้หลังเปรียบเทียบกับ D1 โดยให้ความสำคัญกับงบเพิ่ม 0 บาทและลดงานพัฒนาระบบหลังบ้าน:

- Cloudflare ให้บริการหน้าเว็บและใช้โดเมนเดิม; ไม่ต้องซื้อ Supabase Custom Domain สำหรับ API ใช้ hostname มาตรฐานของโปรเจกต์ได้
- PostgreSQL เก็บข่าว แหล่งข้อมูล การตั้งค่า usage ledger และ audit log
- Supabase Auth ระบุตัวตน Admin และตรวจสิทธิ์ทั้ง API, Edge Functions และ RLS; ซ่อนเมนูใน UI อย่างเดียวไม่ถือว่าป้องกันข้อมูลแล้ว
- ไม่ส่ง service-role credentials ไป browser; ผู้อ่านทั่วไปเข้าถึงเฉพาะ published data ผ่านสิทธิ์ที่กำหนด
- API key ของ Agent เก็บใน Vault และถอดรหัสเฉพาะ backend ที่จำเป็น ปิดสิทธิ์เข้าถึง decrypted view จากผู้อ่านและ browser โดยตรง
- ใช้ Supabase Cron (`pg_cron` ร่วมกับ `pg_net`) เรียก Edge Functions ผ่านการยืนยันตัวตน ไม่มี public endpoint ที่เริ่มงานแพงได้โดยไม่ตรวจสิทธิ์
- cron และ Run now ใช้ run key, lease, checkpoint และ atomic budget reservation ใน PostgreSQL ชุดเดียวกัน
- AI provider ยังไม่เลือกขั้นสุดท้าย ต้องเป็น Free quota ที่ยืนยันได้และผ่านการทดสอบภาษาไทย; Supabase ไม่ได้รวมค่า inference ของ provider ภายนอก

D1 และ Cloudflare Access ไม่อยู่ใน stack ที่เลือกสำหรับ MVP จึงไม่สร้างฐานข้อมูลซ้ำหรือระบบ Auth สองชุด UI prototype ยังใช้ข้อมูลจำลอง การเชื่อมบริการจริงเป็นขั้นถัดไปหลังตรวจต้นแบบ

### 11.2 เงื่อนไขไม่เพิ่มค่าใช้จ่ายและประมาณการพื้นที่

ข้อกำหนดจากผู้ใช้: ไม่อยากเสียเงินเพิ่ม เป้าหมายจึงเป็นค่าโฮสต์/ฐานข้อมูล/AI เพิ่มเติม 0 บาทภายใต้ free quotas ของบริการที่ใช้ โดยไม่รวมค่าต่ออายุโดเมนเดิม การมีบัญชี Cloudflare ไม่ได้ยืนยันว่าได้เปิดผลิตภัณฑ์หรือแพ็กเกจใดไว้ ต้องตรวจบัญชีจริงก่อน deploy

**ปรับเมื่อ 22 กันยายน 2026 (ticket 0001):** โฮสต์ ฐานข้อมูล Storage และ Auth ยังต้องอยู่ใน free quota และกฎ 0 บาทข้างล่างยังบังคับกับบริการเหล่านั้นตามเดิมทุกข้อ **ยกเว้นค่า AI อย่างเดียว** ซึ่งอนุมัติให้จ่ายได้ไม่เกิน **1 USD ต่อเดือน** กับ DeepSeek หลัง ticket 0002 พบว่าไม่มี provider ฟรีรายใดผ่านเกณฑ์ "โควตายืนยันได้ + ทดสอบภาษาไทย" พร้อมกันเลย และ ticket 0006 วัดแล้วว่างบเท่านี้ซื้อได้ 6–48 ข่าวต่อรอบขึ้นกับช่วงเวลา ซึ่งเกินความต้องการจริงที่ ~5–6 ข่าว

โควตาที่ตรวจจากเอกสารผู้ให้บริการวันที่ 20 กันยายน 2026:

| บริการ | Free quota ที่เกี่ยวข้อง |
|---|---|
| Supabase database | 500 MB ต่อโปรเจกต์ รวมข้อมูล/index; ต้องวัดพื้นที่ส่วนระบบและ Auth จริง |
| Supabase Storage | 1 GB สำหรับไฟล์ แยกจากโควตาฐานข้อมูล |
| Supabase egress | 5 GB และ cached egress อีก 5 GB ต่อเดือน เป็นคนละโควตา |
| Supabase Auth | 50,000 monthly active users |
| Supabase Edge Functions | 500,000 invocations ต่อเดือน; CPU 2 วินาทีต่อ request และ wall-clock สูงสุด 150 วินาทีบน Free |
| Supabase project | สูงสุด 2 active Free projects; อาจพักหลัง inactivity 1 สัปดาห์ และไม่มี automatic backups ให้ใน Free |
| Workers (หากใช้ dynamic API เพิ่ม) | 100,000 requests/วัน และ CPU 10 ms ต่อ invocation; เวลารอ network ไม่ใช่ CPU time |
| Pages static assets | requests ของ static assets ฟรีไม่จำกัด หากไม่ได้เรียก Functions |
| Workers AI (ตัวเลือก AI ที่ยังไม่ยืนยัน) | 10,000 Neurons/วันสำหรับโมเดลที่เปิดให้ Free; Neurons ไม่ใช่ token และบางโมเดลต้องใช้ Paid |

การประเมินตัวอย่างสำหรับ Supabase หนึ่งโปรเจกต์: สมมติว่าหลังกันพื้นที่ระบบ/ข้อมูลอื่นแล้วเหลือสำหรับข่าว 400 MB จาก 500 MB และขนาดเฉลี่ยรวมสรุปสองมุมมอง metadata, index และ usage แบบย่อ 20 KB/ข่าว จะเก็บได้ประมาณ 20,000 ข่าว ต้องตรวจ baseline PostgreSQL และขนาดจริงก่อนยืนยัน:

| ข่าวใหม่ที่เก็บต่อวัน | เวลาจนใช้พื้นที่ข่าว 400 MB |
|---|---|
| 20 | ประมาณ 1,000 วัน หรือ 2.7 ปี |
| 50 | ประมาณ 400 วัน หรือ 13 เดือน |
| 100 | ประมาณ 200 วัน หรือ 6–7 เดือน |

นี่เป็นแบบจำลองโดยใช้หน่วย decimal ไม่ใช่ผล benchmark หรือการรับประกันอายุระบบ ไม่รวมไฟล์รูป วิดีโอ raw HTML และ prompt/response logs ที่เก็บสะสมตลอดไป ถ้าบทความเฉลี่ย 40 KB ระยะเวลาข้างต้นจะเหลือครึ่งหนึ่ง และ quota ที่แชร์กับงานอื่นในบัญชีอาจทำให้พื้นที่เหลือน้อยลง

แผนเริ่มต้นที่เสนอ:

- เก็บข้อความสรุปและลิงก์ต้นฉบับ ไม่ mirror บทความ รูป และวิดีโอทั้งหมด
- จำกัดจำนวนข่าวที่จะส่งให้ AI และเริ่มจากแหล่งข้อมูลน้อยเพื่อวัด bytes/ข่าว, CPU, function invocations, egress และ AI quota ต่อข่าวที่ผ่านเกณฑ์
- เสนอ retention ของ raw source content 7 วัน และ traces รายละเอียด 30 วัน พร้อมเก็บ usage แบบตัวเลข/audit ที่จำเป็น; ระบุ retention ชัดก่อนเปิด cleanup จริง
- ยังไม่ลบข่าวเผยแพร่อัตโนมัติ เมื่อพื้นที่ใกล้เต็มให้เสนอ export เก็บในเครื่องหรือเลือกลดประวัติ โดยไม่แอบลบหรือซื้อพื้นที่
- จำกัด query results, ใช้ index ตามงานจริง และ cache หน้าที่อ่านบ่อย เพราะอาจชนโควตาประมวลผลก่อนพื้นที่เต็ม
- เลือก AI provider จากคุณภาพภาษาไทยที่วัดเองจริง ไม่ใช่จากว่าฟรีหรือไม่ หากคุณภาพไม่ผ่านให้คงร่าง/ส่งตรวจ ไม่ลดเกณฑ์คุณภาพเพื่อให้เข้ากับโมเดลที่ถูกกว่า (ticket 0006 วัดแล้ว: DeepSeek ผ่าน)
- API key ของ provider ภายนอกไม่ถือว่าฟรีโดยอัตโนมัติ ต้องมีเพดานงบเป็นตัวเลขที่บังคับได้จริง และทุกการเรียกที่คิดเงิน รวม connection test และ retry ต้องถูกนับในเพดานนั้น; ไม่มีงบที่จองได้ให้ไม่เรียก
- แยกการวัด token, Neurons และเงินที่ต้องจ่ายจริง โควตาแต่ละประเภทใช้แทนกันไม่ได้; daily Cloudflare quota reset ที่ 00:00 UTC ต่างจากรายงานวันไทย
- เมื่อใกล้หมดโควตาให้หยุดรับงานใหม่และพัก backlog จน quota reset; storage เต็มต้องลดข้อมูลหรือ export ไม่ได้ reset เอง หาก Supabase ถูกจำกัด/พักให้แสดงข่าวฉบับ static/cache ที่เตรียมไว้และสถานะอัปเดตล่าช้าเมื่อทำได้
- ก่อน deploy ต้องพิสูจน์ว่า feed parsing, rendering, authentication และ AI pipeline อยู่ในข้อจำกัดของ runtime ที่เลือก; Supabase Edge Functions ต้องผ่าน CPU/wall-clock limits และหากใช้ Workers dynamic API ต้องผ่าน CPU 10 ms ด้วย
- Free Supabase อาจพักเมื่อไม่มีการใช้งานหรือเข้าสู่ read-only เมื่อฐานข้อมูลเกิน 500 MB; จัดแผน export/restore และไม่รับประกัน uptime ของ free tier
- ไม่เปิด paid plan, prepaid credits หรือ automatic paid fallback ของ Cloudflare/Supabase เพื่อแก้ quota เต็ม · ค่า AI เป็นข้อยกเว้นเดียว และถูกคุมด้วย `private.budgets.cash_limit` ($1/เดือน) ที่ระดับฐานข้อมูล: เปิด `ai_enabled` โดยไม่มีงบหรือไม่มี credential ที่ใช้งานได้ เป็นไปไม่ได้ ไม่ใช่แค่ไม่ควร

Free plan ไม่กำหนดระยะทดลองแบบจำนวนเดือนตามเอกสารที่ตรวจ แต่ขึ้นกับการใช้โควตาและเงื่อนไขบริการ จึงไม่รับประกันว่าจะเก็บข้อมูลทุกประเภทไปตลอดโดยไม่มีค่าใช้จ่าย

## 12. แบบจำลองข้อมูลระดับผลิตภัณฑ์

| กลุ่มข้อมูล | หน้าที่ |
|---|---|
| Sources / People | แหล่งข้อมูล บุคคล องค์กร และการตั้งค่าการดึงข่าว — `private.sources` เก็บ `source_key` (ที่ `story_records` อ้างถึงด้วย FK), `feed_url`, `fetch_mode`, ธง `commentary` และสวิตช์ `enabled` · มีช่องสำหรับสถิติความล้มเหลวรายแหล่ง (`consecutive_failures`, `last_ok_at`, `last_failure_code`) ซึ่ง**ขั้นดึงข้อมูลเป็นผู้เขียน และยังไม่มีใครเขียนจนกว่าขั้นนั้นจะสร้างเสร็จ** · แถว `manual` คือโต๊ะบรรณาธิการ ไม่มี feed และไม่ถูกดึง |
| Fetch Allowlist | `private.fetch_allowlist` — โดเมนที่อนุญาตให้ตามลิงก์ไปดึงหน้าเดียว หนึ่งโดเมนหนึ่งแถวพร้อมเหตุผล แยกจากรายชื่อ feed เพราะจำนวนไม่ตรงกันทั้งสองทาง |
| Source Items | URL, วันที่ต้นฉบับ, hash และสถานะการดึงรายการต้นทาง |
| Stories / Citations | ข่าวที่รวมเรื่องซ้ำแล้ว ข้อเท็จจริง แหล่งอ้างอิง และเนื้อหาสองระดับ |
| Revisions / Reviews | เวอร์ชันบทความ การตรวจอนุมัติ การแก้ไข และการถอนเผยแพร่ |
| Editorial Policies / Decisions | กฎคัดข่าว โหมดรายแหล่ง/หมวด เวอร์ชันนโยบาย และเหตุผลที่แต่ละข่าวผ่าน/ถูกตัดออก |
| Editions | ฉบับเช้า/เย็นและลำดับข่าวที่อ้างอิง |
| Tool Profiles / Playbooks | ข้อมูลเครื่องมือและวิธีทำงาน พร้อมวันที่ตรวจสอบและระดับหลักฐาน |
| Agent Configs / Credential Refs | การตั้งค่า Agent และ reference ไปยัง secret ที่เก็บแยก |
| Runs / Steps | สถานะงาน checkpoint, lease และข้อมูลการกู้คืน |
| Invocations / Pricing / Budgets | usage ต่อ attempt ราคาขณะเรียก และการสำรอง/กระทบยอดงบ |
| Audit Events | ประวัติการกระทำของ Admin ที่ไม่บันทึก secrets |

เผยแพร่เฉพาะข้อมูลสถานะ published ให้ผู้อ่านทั่วไปเข้าถึง ข้อมูล draft, usage, credentials และ logs จำกัดสิทธิ์ฝั่ง backend และฐานข้อมูล

## 13. ข้อกำหนดด้านคุณภาพและการปฏิบัติการ

- หน้าอ่านข่าวต้องทำงานได้ขณะ worker ประมวลผล และใช้ cache สำหรับเนื้อหาเผยแพร่
- ใช้ server-side search ที่รองรับภาษาไทยตามผลทดสอบจริง ไม่สมมติว่า tokenizer เริ่มต้นของฐานข้อมูลเพียงพอ
- การดึง URL จากแหล่งข่าวต้องป้องกันการเข้าถึง private network ตรวจ redirect จำกัดขนาดข้อมูลและเวลา
- แสดงเนื้อหาผ่าน renderer ที่ปลอดภัย ไม่รัน HTML หรือ script จากต้นฉบับ
- จำกัดสิทธิ์ Admin และตรวจ authorization ทุก endpoint ที่แก้การตั้งค่าหรือเริ่มงาน
- ข้อมูลจากแหล่งภายนอกต้องไม่มีสิทธิ์เรียกเครื่องมือ เปลี่ยน prompt หรืออ่าน secret ผ่านคำสั่งที่ฝังมา
- แจ้งปัญหาในหลังบ้าน: cron ไม่สำเร็จ credential ใช้งานไม่ได้ งบเต็ม หรือมีข่าวรอตรวจ
- ตั้งค่า retention ของ raw source content, logs และ traces; ไม่เก็บเนื้อหาที่ไม่จำเป็นถาวร
- ก่อน production ต้องกำหนด backup/restore, ผู้รับผิดชอบ และทดสอบการกู้คืนตามแพ็กเกจที่เลือก
- ประเมินเวลาโหลดและความจุด้วยข้อมูลเริ่มต้นที่ตกลงก่อนกำหนด SLA; MVP นี้ยังไม่สัญญาความพร้อมใช้งานเชิงพาณิชย์

## 14. Acceptance criteria

| ID | เกณฑ์ที่ตรวจสอบได้ |
|---|---|
| AC-01 | ผู้อ่านสลับสองมุมมองได้โดยไม่สมัครสมาชิก และเปิดข่าวเรื่องเดิมโดยข้อเท็จจริงไม่เปลี่ยน |
| AC-02 | ข่าวที่เผยแพร่ทุกเรื่องมีประเภทเนื้อหา แหล่งอ้างอิง และเวลาที่มารองรับ; วันที่ที่หาไม่ได้ระบุว่าไม่ทราบ |
| AC-03 | ส่วนเทคนิคสร้างเฉพาะเรื่องที่เกี่ยวข้อง และมีหลักฐานสำหรับข้อกล่าวอ้างด้านประสิทธิภาพหรือเวอร์ชัน |
| AC-04 | scheduler ตั้งเวลา 07:00/19:00 ไทยถูกต้อง และบันทึกเวลารันจริงแยกจากเวลานัดหมาย |
| AC-05 | รันซ้ำด้วย source item เดิมไม่สร้างข่าวหรือยอด usage จาก invocation เดียวซ้ำ |
| AC-06 | แหล่งข้อมูลหนึ่งล้มเหลวไม่ขัดขวางข่าวที่สำเร็จ และรอบแสดง partial พร้อมสาเหตุ |
| AC-07 | worker หยุดระหว่างทางแล้วกู้คืนได้โดยไม่เผยแพร่ข่าวซ้ำ; invocation ที่สถานะไม่แน่นอนไม่ถูกนับเป็นศูนย์ |
| AC-08 | Admin เปลี่ยน Agent/model/key และทดสอบได้ โดย browser/logs ไม่เห็น key เต็มหลังบันทึก |
| AC-09 | รายงานแยก provider-reported, estimated และ unavailable และรองรับ retry, cached และ reasoning usage โดยไม่ซ้ำ |
| AC-10 | ยอดรวมสองมุมมองตรงกับ usage ledger และรายงานราคาประวัติไม่เปลี่ยนเมื่อแก้ราคาปัจจุบัน |
| AC-11 | งานพร้อมกันต้องสำรองงบโดยไม่ใช้วงเงินเดียวกันซ้ำ และเมื่อถึงเพดานไม่มี request ใหม่เริ่มนอกนโยบาย |
| AC-12 | การเผยแพร่อัตโนมัติข้ามรายการที่ไม่ผ่านเกณฑ์; การอนุมัติและแก้ข้อเท็จจริงมี audit trail |
| AC-13 | ชุดข่าวทดสอบมีข่าวซ้ำ แหล่งขัดแย้ง วันที่ขาด และ prompt injection; กรณีที่ไม่ปลอดภัยถูกส่งตรวจหรือปฏิเสธ |
| AC-14 | ตัวกรอง stack และค้นหาภาษาไทยให้ผลตามชุดทดสอบที่กำหนด และใช้งานหลักได้บนมือถือ/keyboard |
| AC-15 | หน้าเว็บคงข่าวเดิมและเวลาอัปเดตสำเร็จเดิมเมื่อรอบใหม่ล้มเหลว |
| AC-16 | ไม่มีป้ายทดลองภายในหรือคำกล่าวอ้างว่าเร็วขึ้น/ประหยัดเป็นเปอร์เซ็นต์โดยไม่มีบันทึกผลรองรับ |
| AC-17 | Manual ไม่เผยแพร่ก่อน Admin อนุมัติ; Auto เผยแพร่เฉพาะที่ผ่านเกณฑ์; Hybrid ใช้นโยบายรายแหล่ง/หมวดและให้ Manual ชนะเมื่อกฎขัดกัน |
| AC-18 | ทดลองกฎได้โดยไม่เผยแพร่จริง รายการผ่าน/ตัดออก/รอตรวจมีเหตุผล และ usage ที่เกิดจากการทดลองถูกรวมในงบ |
| AC-19 | ปิด Auto แล้วงานที่ยังไม่เผยแพร่หยุดที่ร่าง แม้รอบนั้นเริ่มก่อนปิด; บทความที่ปฏิเสธหรือถอนจะไม่กลับมาจากการรันซ้ำ |
| AC-20 | เปลี่ยนกฎแล้วร่างค้างไม่เผยแพร่ย้อนหลังทันที และการแก้ข้อเท็จจริงสำคัญของข่าวเผยแพร่แล้วต้องผ่าน revision ที่ตรวจได้ |
| AC-21 | โหมดงบศูนย์ไม่ส่ง request ที่อาจคิดเงิน รวม retry/test/fallback; ถึง free quota แล้วพักงานพร้อมสถานะ โดยไม่เปิดบริการเสียเงิน |
| AC-22 | ก่อน deploy มีผลวัด CPU/wall-clock, bytes/ข่าว, function invocations, egress และ AI quota จาก workload ตัวอย่างจริง พร้อมยืนยันว่าเส้นทางหลักอยู่ใน Free limits |
| AC-23 | หน้าอ่านข่าวทั้งสองมุมมองและหลังบ้านใช้ daisyUI/theme ร่วมกัน องค์ประกอบหลักไม่สร้าง CSS ซ้ำโดยไม่มีเหตุผล และเส้นทางใช้งานเดิมยังทำงานบน desktop/mobile/keyboard |

ใช้การทดสอบอัตโนมัติสำหรับ logic และเส้นทางสำคัญร่วมกับการตรวจบรรณาธิการจากตัวอย่างจริง การผ่าน schema หรือการให้โมเดลตรวจโมเดลอีกครั้งไม่ถือเป็นหลักฐานว่าข้อเท็จจริงถูกทั้งหมด

## 15. ตัวชี้วัด

- ประโยชน์ต่อผู้อ่าน: feedback ว่าเข้าใจข่าวหรือใช้ตัดสินใจได้ และรายการที่บันทึกอ่าน/ทดลอง
- คุณภาพ: สัดส่วนรายการที่ผ่านการตรวจ จำนวนการแก้ข้อเท็จจริง และข่าวซ้ำที่หลุดเผยแพร่
- ความสด: เวลาจากพบข่าวถึงพร้อมตรวจ/เผยแพร่ และอัตรารอบที่ทำงานสำเร็จ
- ประสิทธิภาพ: token และต้นทุนต่อข่าวที่ผ่านเกณฑ์ จำนวน retry และเวลาแก้ไขของคน
- ความเกี่ยวข้อง: feedback แยกมุมมองทั่วไปและนักพัฒนา

ช่วงเริ่มต้นเก็บ baseline ก่อนตั้งเป้าหมายการลดต้นทุนหรือเวลา ไม่รับประกันเปอร์เซ็นต์ประหยัดล่วงหน้า

## 16. ขอบเขตการส่งมอบ

### MVP

- หนังสือพิมพ์สองมุมมองจากฐานข่าวร่วมกัน พร้อมค้นหาและตัวกรอง
- หน้ารายละเอียดข่าว แหล่งอ้างอิง และฉบับสรุปเช้า/เย็น
- Tools Radar และ Playbook ในรูปแบบบทความที่มีโครงสร้างและบรรณาธิการตรวจ
- แหล่งข้อมูลที่คัดเลือก cron ทุก 12 ชั่วโมง และหลังบ้านตรวจเผยแพร่
- กฎกรองข่าว โหมด Manual/Auto/Hybrid คิวตรวจ เหตุผลการตัดสิน และสวิตช์หยุด Auto
- ตั้งค่า Agent/API key และ usage ledger พร้อมควบคุมงบ
- ประวัติรัน การจัดการความผิดพลาด และแนวทางประเมินคุณภาพ

### ระยะถัดไปตามข้อมูลการใช้งาน

- ตรวจข่าวใหม่ทุกชั่วโมงและแจ้งเตือนเรื่องสำคัญ
- บัญชีผู้อ่านเพื่อ sync stack, bookmarks และรายการทดลองข้ามอุปกรณ์
- หน้าติดตามเหตุการณ์ต่อเนื่องและเปรียบเทียบเครื่องมือเชิงลึก
- workspace สำหรับบันทึก benchmark ที่ทดลองจริง
- เชื่อม telemetry จากเครื่องมือพัฒนาภายนอกผ่านการตั้งค่าเฉพาะ
- newsletter และช่องทางแจ้งเตือนที่ผู้ใช้เลือกสมัคร

ยังไม่รวมใน MVP: ระบบสมาชิกจ่ายเงิน การรันโค้ดจากข่าวอัตโนมัติ การเชื่อมทุกโซเชียลแพลตฟอร์ม และ autonomous agents ที่ปรับแก้โปรเจกต์ของผู้อ่าน

## 17. เอกสารทางการประกอบข้อเสนอ

- [Cloudflare Next.js](https://developers.cloudflare.com/workers/framework-guides/web-apps/nextjs/): เส้นทาง deploy และ compatibility ที่ต้องตรวจ
- [Cloudflare Cron Triggers](https://developers.cloudflare.com/workers/configuration/cron-triggers/): งานตามเวลา
- [Workers limits](https://developers.cloudflare.com/workers/platform/limits/): CPU และ runtime limits ตามแพ็กเกจ
- [Custom Domains](https://developers.cloudflare.com/workers/configuration/routing/custom-domains/): การผูกโดเมนและเงื่อนไข DNS zone
- [D1](https://developers.cloudflare.com/d1/): ฐานข้อมูล managed ที่ใช้ SQL แบบ SQLite
- [D1 limits](https://developers.cloudflare.com/d1/platform/limits/): ขนาดต่อฐานข้อมูลและพื้นที่รวม
- [D1 pricing](https://developers.cloudflare.com/d1/platform/pricing/): โควตาการอ่าน/เขียนและพฤติกรรมเมื่อเต็ม
- [Pages pricing](https://developers.cloudflare.com/pages/functions/pricing/): static requests กับ Functions
- [Workers AI pricing](https://developers.cloudflare.com/workers-ai/platform/pricing/): Neurons และ free allocation
- [Workers AI paid-only models](https://developers.cloudflare.com/changelog/post/2026-07-28-models-require-workers-paid/): ข้อจำกัดการเข้าถึงบางโมเดลบน Free
- [Cloudflare Access paths](https://developers.cloudflare.com/cloudflare-one/access-controls/policies/app-paths/): การป้องกัน hostname และ path
- [Workers Secrets](https://developers.cloudflare.com/workers/configuration/secrets/): secret bindings สำหรับ Worker
- [Supabase Vault](https://supabase.com/docs/guides/database/vault): encrypted secrets และสิทธิ์เข้าถึง decrypted view
- [Supabase pricing](https://supabase.com/pricing): Free quotas, inactivity และข้อจำกัด backup
- [Supabase Edge Function limits](https://supabase.com/docs/guides/functions/limits): CPU และระยะเวลาทำงาน
- [Supabase Edge Function pricing](https://supabase.com/docs/guides/functions/pricing): invocation quota
- [Supabase scheduling](https://supabase.com/docs/guides/functions/schedule-functions): cron เรียก Edge Functions
- [Supabase database size](https://supabase.com/docs/guides/platform/database-size): database quota และ read-only mode
- [Supabase cost controls](https://supabase.com/docs/guides/platform/cost-control): Free plan restrictions โดยไม่คิดค่า overage
- [AI SDK provider management](https://ai-sdk.dev/docs/ai-sdk-core/provider-management): การจัดการ provider และ model
- [Zod](https://zod.dev/basics): schema และ runtime validation
- [Promptfoo](https://www.promptfoo.dev/docs/getting-started/): evaluation ของ prompt และ model
- [Langfuse token and cost tracking](https://langfuse.com/docs/observability/features/token-and-cost-tracking): usage ประเภทต่าง ๆ และราคาที่ใช้คำนวณ
- [Playwright assertions](https://playwright.dev/docs/test-assertions): การทดสอบพฤติกรรมหน้าเว็บ
- [Anthropic: Code execution with MCP](https://www.anthropic.com/engineering/code-execution-with-mcp): การโหลดเครื่องมือเมื่อจำเป็นและลดข้อมูลที่ส่งเข้า context

ก่อนพัฒนาหรือ deploy ให้ตรวจเวอร์ชัน ราคา และข้อจำกัดของบริการที่เลือกอีกครั้ง โดยไม่ใส่ API key ลงในเอกสารนี้
